"""
Deprecated static job source registry.

This module is kept only as a reference and seed source while the runtime
uses the database-backed job_sources table.
"""

from src.domain.jobs.constants import JOB_SOURCE_REGISTRY
