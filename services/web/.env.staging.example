# Staging environment template.
# Set these as environment variables on the staging host/CI — do NOT commit .env.staging.

# --- API ---
NEXT_PUBLIC_API_URL=https://api.staging.artemis.dev
NEXT_PUBLIC_APP_ENV=staging
