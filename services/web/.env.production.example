# Production environment template.
# Set these as environment variables on the production host/CI — do NOT commit .env.production.

# --- API ---
NEXT_PUBLIC_API_URL=https://api.artemis.dev
NEXT_PUBLIC_APP_ENV=production
