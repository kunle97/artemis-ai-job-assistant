# Local development defaults — safe to commit, no real secrets.
# Next.js loads this automatically when NODE_ENV=development.

# --- API ---
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_ENV=development
